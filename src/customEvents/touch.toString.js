// import { stringCoords, stringElement } from './helpers';

export default function() {
  let str = `${this.type}`;
  // str += stringCoords(this.screenX, this.screenY);
  // str += stringElement(this);
  return str;
}
